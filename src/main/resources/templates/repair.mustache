<html>

<body>
    <div>
        <form action="/logout" method="post">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
            <input type="submit" value="Sign Out"/>
        </form>
    </div>
    <div>
        <form method="post">
            <<input type="number" name="priority" placeholder="Введите приоритет" />
            <input type="text" name="status" placeholder="Укажите статус" />
            <input type="text" name="description" placeholder="Описание" />
            <input type="text" name="username" placeholder="Введите имя рабочего" />
            <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
            <button type="submit">Добавить</button>
        </form>
    </div>
    <!--<div>Список сообщений</div>
    <form method="post" action="fio">
        <input type="text" name="fio">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <button type="submit">Найти</button>
    </form>-->
    {{#repairs}}
    <div>
        <b>{{id}}</b>
        <b>{{priority}}</b>
        <b>{{status}}</b>
        <b>{{description}}</b>
        <b>{{username}}</b>
    </div>
    {{/repairs}}
</body>
</html>